# 著作權與隱私稽核

這份文件記錄本專案對第三方內容與患者隱私的處理原則、實際做法，以及稽核結果。

稽核日期：**2026-08-02**

---

## 一、本專案對第三方內容做了什麼

| 行為 | 有沒有做 |
|---|---|
| 儲存原始頁面連結 | ✅ 有 |
| 儲存公開中繼資料（標題、頻道、長度、觀看數） | ✅ 有 |
| 官方允許的 embed（YouTube `youtube-nocookie.com`） | ✅ 有 |
| YouTube timestamp deep link | ✅ 有 |
| 課程頁面列出建議觀看區間（`timestamps` 欄位） | ✅ 有 |
| 自行撰寫繁體中文重點摘要（`why` / `target` 欄位） | ✅ 有 |
| 以文字描述應觀察的影像特徵 | ✅ 有 |
| **下載影片** | ❌ 沒有 |
| **剪輯或擷取片段** | ❌ 沒有 |
| **重新上傳或代管任何影音** | ❌ 沒有 |
| **代管任何患者影像** | ❌ 沒有 |
| **截圖繞過原網站的存取限制** | ❌ 沒有 |
| **重製受著作權保護的指引全文** | ❌ 沒有（只連結到原始發布頁面） |

`dist/` 建置產物中**沒有任何影片檔、音訊檔或醫學影像檔**。
可用下列指令自行驗證：

```bash
make build
find dist -type f \( -name '*.mp4' -o -name '*.webm' -o -name '*.dcm' \
  -o -name '*.jpg' -o -name '*.png' -o -name '*.gif' \) -not -name 'og.png' -not -name 'favicon*'
```

唯一的圖片產物是 `og.png`（本站自行產生的社群預覽圖）與 favicon。

---

## 二、什麼情況下才可以真正擷取片段

本專案**目前一個片段都沒有擷取**。若未來要擷取，必須滿足下列任一條件並在此文件留下紀錄：

1. 明確屬於 public domain
2. 採允許重製或改作的 Creative Commons license（CC BY / CC BY-SA / CC0）
3. 已取得權利人書面授權
4. 符合法域內可確認的合理使用條件

**授權狀況不明時，絕對不下載也不重新上傳。**

---

## 三、授權標示規則

每個資源的 `license` 欄位必須填寫，規則如下：

| 情境 | `license` 寫法 |
|---|---|
| YouTube 第三方頻道 | `版權歸原頻道，本站僅連結` |
| 學會發布的公開指引 | `版權歸 <學會全名>，本站僅連結原始發布頁面` |
| 開放取用期刊文章 | 照抄該文標示的授權（例如 `CC BY 4.0`、`CC BY-NC-ND 4.0`） |
| 訂閱制期刊 | `訂閱制內容，本站僅連結摘要／DOI` |
| 病例圖譜（如 Radiopaedia） | 照抄該站標示的授權與作者 |
| 廠商教育內容 | `版權歸 <廠商>，本站僅連結`＋必須另填 `coi` |
| **無法確認** | `授權狀況未確認，僅連結不重製` |

**「無法確認」是允許的答案，猜一個授權條款不是。**

`make audit` 會檢查：所有非 YouTube 來源必須同時具備
`source_type`、`access`、`license`、`last_verified`，缺任一個即報錯。

---

## 四、存取限制的標示

需要註冊、訂閱或機構帳號的來源，一律在 `access` 欄位標示，並在網站上以醒目標籤顯示：

| `access` | 網站顯示 | 意義 |
|---|---|---|
| `open` | 不顯示標籤 | 公開可取用 |
| `registration` | 需免費註冊 | 要建帳號但不用付費 |
| `subscription` | 需訂閱 | 要付費或期刊訂閱 |
| `institutional` | 需機構帳號 | 要醫院／大學的存取權 |

`src/build/verify_external.py` 會偵測頁面上的登入牆關鍵字（`sign in to continue`、
`subscription required`、`institutional login` 等），偵測到但 `access` 標成 `open` 的，
會在驗證報告裡列出來要求人工修正。

**理由：** 讓學員在點下去之前就知道會不會撞牆，是課程對使用者的基本誠實。

---

## 五、利益關係揭露

設備廠商（GE、Philips、Siemens、Canon、Samsung、Mindray 等）製作的教育內容
**可以**納入操作教學，但必須：

1. 在 `coi` 欄位寫明廠商關係，網站上以紅色標籤顯示
2. **不得**把產品宣傳視為臨床證據
3. 其中的診斷與治療主張必須另外以指引或同儕審查文獻查核

講者與廠商有顧問、演講費或股權關係且該關係公開可知的，也一併寫進 `coi`。

**限制：** 我們只能標示**公開可查**的利益關係。多數 YouTube 影片與會議錄影不會揭露
講者的財務關係，這部分本專案**無法查核**——列在 `KNOWN-GAPS.md`。

---

## 六、患者隱私

### 納入前必須確認

- 已去識別化
- 來源合法
- 沒有姓名、病歷號、生日或其他可識別資訊
- 沒有可辨識的面容
- 不以截圖繞過原網站的存取限制

含可識別患者資料的資源**一律排除**，並在 `registry-ch*.json` 記錄排除理由。

### 本專案的結構性保護

本站**不代管任何患者影像**。所有醫學影像都留在原始來源的伺服器上，
使用者點進去看的是原機構自己發布、自己負責去識別化的內容。
這在架構上就消除了本專案二次散布患者資料的可能。

### 限制

我們**無法**驗證原始來源的去識別化是否徹底，也無法驗證原機構是否取得患者同意。
我們能做的是：（一）不重製，（二）看到明顯可識別資訊就排除，（三）在此誠實說明限制。

---

## 七、本專案自身的授權

| 項目 | 授權 |
|---|---|
| 框架程式碼（`src/`） | MIT，見 `LICENSE`。衍生自 [htlin222/gym-course](https://github.com/htlin222/gym-course) |
| 課程結構、單元設計、評量題庫、說明文件 | 本專案原創，同 MIT |
| Lucide 圖示 | ISC |
| **所有連結指向的影音、病例、指引與文獻** | **版權歸原權利人所有，不在本專案授權範圍內** |

---

## 八、稽核結果

| 檢查項目 | 方法 | 結果 |
|---|---|---|
| 建置產物含影音檔 | `find dist -name '*.mp4' …` | 見上方指令，應為空 |
| 所有非 YouTube 來源具備 `license` | `make audit` | 缺任一即報錯，交付時零錯誤 |
| 所有非 YouTube 來源具備 `access` | `make audit` | 同上 |
| 登入牆與 `access` 標示是否一致 | `make verify-external` | 不一致者列於驗證報告 |
| 廠商內容是否標示 `coi` | 人工逐筆檢查 `registry-ch*.json` | 見各章 registry |
| 可識別患者資料 | 策展時人工逐筆檢查 | 排除紀錄見 `registry-ch*.json` |
| `last_verified` 是否過舊 | `make audit`（門檻 180 天） | 超過即警告 |

### 仍需人工確認的部分

1. **講者財務利益關係**：多數公開影音不揭露，無法查核。
2. **原始來源的去識別化品質**：本專案不重製，但也無法為原機構的處理背書。
3. **授權標示為「未確認」的資源**：這些連結是安全的（只是連結），
   但若未來要引用其內容或截圖，必須先取得授權。
4. **合理使用的法域差異**：本專案的使用者可能在不同法域，本文件不構成法律意見。

以上四點列入 `KNOWN-GAPS.md`。
