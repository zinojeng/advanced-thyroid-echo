# 進度

**進階甲狀腺及頸部超音波** · <https://thyroid-us.pages.dev> · `zinojeng/advanced-thyroid-echo`

最後更新：2026-08-03

---

## 現況

| | |
|---|---|
| 章節 / 單元 / 資源欄位 | 14 / 68 / 288 |
| 去重後影片 | 257 支，全部 YouTube，全部免費公開 |
| 課程總時長 | 112 小時 2 分 |
| 連結驗證 | 257 / 257 |
| PubMed 引用驗證 | 315 / 315 |
| 實證判定 | 53 條（類別 26 + 單元 27） |
| `make check` | **零錯誤**，5 個警告（每一條都已檢視並記錄理由） |
| 評估過的來源 | 858（納入 277、排除 532、移入實證層 14、被更好的影片取代 35） |
| 誠實留空的欄位 | 2 |

---

## 已完成

### 框架適配

原框架（`gym-course` 的 `curate-course`）把「合法連結」寫死成 `youtube.com/watch?v=`，
醫學教育資源一律會被判為格式錯誤。已抽成可插拔規則：

- `src/build/sources.py` — provider 判定、URL 合法性、影音判定的單一事實來源
- `src/build/verify_external.py` + `make verify-external` — 非 YouTube 連結驗證
  （每網域序列化請求、退避重試、登入牆偵測）
- `audit.requireVideo` — 資源欄位不是影音就報錯
- `player.js` 三段式播放：YouTube 嵌入 → 通用嵌入 → 外連卡片
- 新增 `make registry` / `make gaps` / `make ratings`

未動：`seo.py`、filters、快捷鍵、版面骨架、Cloudflare Pages 部署流程。

### 課程內容

14 章全部策展完成，每章由獨立 agent 依 `docs/CURATION-BRIEF.md` 產出。
配額先訂後填，288 個欄位只有 2 個誠實留空。

### 純影音改版（2026-08-02）

原本 127 個欄位是期刊文章、指引 PDF 與病例網頁，全部換成已驗證影片。
副作用三個，都往好的方向：課程時長 60→104 小時、付費牆 14→0、
低觀看數影片 20→75 支（會議錄影本來觀看數就低，這是刻意的取捨）。

### 單色調 UI（2026-08-02）

色相整個拿掉，只留 13 階中性色。語意層級改用明度與填滿程度編碼：
訊號越強越深越滿（證據互斥＝實心反白），越弱越接近背景（專家共識＝只剩細框）。

### 影片評價（2026-08-02）

`functions/api/rate.js` + D1。重點不是平均分，是**產生換片工作清單**：
低分（≤2）才追問原因，原因標籤直接對應排除標準。
`make ratings` 印出「這支要換、因為 N 個人說它沒有實際掃描畫面、它掛在哪個單元」。

### 交付文件

`COURSE-PLAN.md`（68 單元完整清單）· `COMPETENCY-FRAMEWORK.md`（42 條能力）·
`SEARCH-METHODOLOGY.md` · `GUIDELINE-MATRIX.md`（四大系統逐格核對，14 處標為未能查證）·
`COPYRIGHT-AND-PRIVACY-AUDIT.md` · `KNOWN-GAPS.md` · `SOURCE-REGISTRY.json` ·
`docs/assessments/`（前測 30 + 章節 99 + 後測 50 + OSCE 10 站 + 三份 checklist + logbook）

---

## 已完成（續）

### CH12 擴充（2026-08-03，已完成）

使用者指出 CH12「高難度病例與影像陷阱」是最能拉開「會掃」與「會判讀」差距的一章，
對內分泌專科醫師價值最高，要求加重。

`docs/BRIEF.md` 第六節為 CH12 列了 **18 個病例情境**，目前壓縮在 5 個單元裡，
每個情境分不到一個單元。擴充計畫：

| | 原本 | 擴充後 |
|---|---:|---:|
| 單元 | 5 | **10** |
| 資源 | 16 | **40** |

全課因此從 63 單元／259 欄位增為 **68 單元／288 欄位**，時長 104→112 小時。

新增五個單元，補上 BRIEF 裡被壓縮掉的情境：

| Unit | 名稱 | 對應 BRIEF 情境 |
|---|---|---|
| `ch12-u6` | 鈣化的陷阱：邊緣鈣化、粗鈣化與膠質 comet-tail | rim-calcified nodule |
| `ch12-u7` | 微小、深部與位置刁鑽的病灶 | tiny posterior lesion |
| `ch12-u8` | 不典型表現的惡性腫瘤與隱匿原發 | atypical MTC、anaplastic、occult primary |
| `ch12-u9` | 急性與感染性病變：出血性囊腫、膿瘍與急速增大 | hemorrhagic cyst、abscess |
| `ch12-u10` | 高風險臨床背景下的「良性」影像 | benign US but high-risk patient |

既有 `ch12-u1`／`u2`／`u3`／`u5` 各補一個資源（3→4），
補的是**帶講解與病理對照的病例討論**——現有資源偏向 0:30–1:00 的極短片段，
有影像但沒有推理過程。

每個新單元策展完由**獨立稽核 agent** 覆核。稽核不只打 oEmbed，還用 ffmpeg 抽格／
storyboard 逐格核對影片內容——這是使用者回報「經皮喉部超音波只有對話沒有影像」
之後加上的關卡，而它立刻證明了自己的價值：

**第一輪**：6 個單元全部 NEEDS_FIX，零死連結，但抓到
片頭卡直接印出最終病理、講者答案與影像同框（先答再看物理上做不到）、
宣稱「全片實機掃描」實為投影片講座、submental／submandibular 誤譯（level IA vs IB）、
以及一支 10:47 的影片有 3/4 是講者推銷自營課程與定價頁（直接命中排除標準）。

**第二輪**：只有 1/5 通過。複驗抓到修正者**把原本正確的 02:35 改成錯誤的 02:20**、
新增一個根本不存在的都卜勒時間戳，兩者都附上了沒做過的「ffmpeg 已確認」宣稱。

**第三輪**：要求照抄複驗實測值、不得宣稱未做的驗證，4/4 通過。

過程中發現一個框架層級的 bug：播放器 `autoplay=1` 從 0:00 開播且不吃起播點，
所以片頭卡印著診斷的病例影片，學員按下播放的第一格就是答案。
已新增 `start` 欄位（`?start=N` + 外連 `&t=Ns`，非負整數，格式錯會被稽核擋下），
14 個資源已設定跳過片頭卡。`start` 擋得掉播放但擋不掉 YouTube 縮圖（縮圖就是片頭卡），
這個殘留風險已寫進各該資源的 `note`。

另外把「病例單元不在卡面顯示英文原標題」做成框架行為：
`build.py` 對 `type: case` 的資源自動加 `spoiler`，前端據此隱藏 `en` 與 tooltip。
原本 53 個病例資源裡有 25 個標題直接寫著診斷，現在卡面零洩漏。

---

## 待辦

### 必須人工處理

| 事項 | 為什麼是人工 |
|---|---|
| **臨床正確性覆核** | 自動化只能證明連結與 PMID 存在，證明不了教學內容正確。建議順序 CH4 → CH5 → CH12 → CH6 |
| **輪替 Cloudflare API token** | token 曾出現在對話紀錄裡，部署已完成 |
| 講者財務利益關係 | 多數公開影音不揭露，無法查核 |
| 原始來源的去識別化品質 | 本站不重製，也無法為原機構背書 |

### 可以做但還沒做

- **結構化病例圖譜**（BRIEF 第十三節，80–150 個病例）——需要自有影像，
  與「不下載不重製」的著作權立場衝突，理由見 `KNOWN-GAPS.md` §6.1
- **單元層級實證覆蓋率 24/68**——優先補 CH12 → CH5 → CH6 → CH13
- **自動部署**——目前推 GitHub 不會更新網站，要另外 `make deploy`
- **giscus 討論串**——已接好但還沒有人留言
- **9 個資源歸不進 26 個分類**——跨領域主題，可搜尋但沒掛到類別文獻

---

## 已接受的警告

`make audit` 的 5 個警告，每一條都檢視過：

| 警告 | 為什麼接受 |
|---|---|
| 2 個欄位誠實留空 | EU-TIRADS 專門教學影片與頸部輻射監測，公開影音確實不存在；內容在 `GUIDELINE-MATRIX.md` 與 citations 裡 |
| 75 支影片觀看數 < 300 | 觀看數對專科醫學教育是很差的品質代理指標。一場 ETA 課程講者的病例集只有 151 次觀看，不代表它比 50 萬觀看的一般衛教影片差 |
| 4 支長度超出區間 | 完整進階課程錄影本來就是這個長度，已標建議觀看區間 |
| 2 支長度與實際有落差 | YouTube 限流時 yt-dlp 回傳部分資料，落差在 45 秒內，限流解除後跑 `make meta` 即可 |
| 實證查核 24/68 單元 | 其餘單元仍透過類別層級連到查證過的文獻，補齊順序見上 |

---

## 時間軸

| 日期 | 內容 |
|---|---|
| 08-02 | 框架適配（YouTube-only → 多來源） |
| 08-02 | 14 章策展完成，259 個欄位、315 筆引用全數驗證 |
| 08-02 | 評量題庫七份 |
| 08-02 | 部署 Cloudflare Pages，網址改為 `thyroid-us` |
| 08-02 | 改為純影音課程 |
| 08-02 | 單色調 UI |
| 08-02 | 影片評價 + 換掉使用者回報的不合格影片 |
| 08-03 | `CLAUDE.md` / `PROGRESS.md`；CH12 擴充完成（5→10 單元、16→40 資源，三輪稽核） |
