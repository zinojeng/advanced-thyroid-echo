# 進度

**進階甲狀腺及頸部超音波** · <https://thyroid-us.pages.dev> · `zinojeng/advanced-thyroid-echo`

最後更新：2026-08-03

---

## 現況

| | |
|---|---|
| 章節 / 單元 / 資源欄位 | 14 / 68 / 288 |
| 去重後影片 | 257 支，全部 YouTube、全部免費公開 |
| 課程總時長 | 112 小時 2 分 |
| 連結驗證 | 257 / 257 |
| PubMed 引用驗證 | 315 / 315 |
| 實證判定 | 53 條（類別 26 + 單元 27） |
| `make check` | **零錯誤**，5 個警告（每一條都已檢視並記錄理由） |
| 評估過的來源 | 858（納入 277、排除 532、移入實證層 14、被更好的影片取代 35） |
| 誠實留空的欄位 | 2 |
| Gemini 內容查核 | 已查 72 / 257 支，標記 **15 支待人工複驗** |

---

## 已完成

### 框架適配

原框架（`gym-course` 的 `curate-course`）把「合法連結」寫死成 `youtube.com/watch?v=`，
醫學教育資源一律會被判為格式錯誤。已抽成可插拔規則：

- `src/build/sources.py` — provider 判定、URL 合法性、影音判定的單一事實來源
- `src/build/verify_external.py` + `make verify-external` — 非 YouTube 連結驗證
- `audit.requireVideo` — 資源欄位不是影音就報錯
- 新增 `make registry` / `gaps` / `ratings` / `feedback` / `verify-content`

### 課程內容

14 章全部策展完成，每章由獨立 agent 依 `docs/CURATION-BRIEF.md` 產出。
配額先訂後填，288 個欄位只有 2 個誠實留空。

### 純影音改版

原本 127 個欄位是期刊文章、指引 PDF 與病例網頁，全部換成已驗證影片。
副作用三個，都往好的方向：時長 60→104 小時、付費牆 14→0、
低觀看數影片 20→75 支（會議錄影本來觀看數就低，這是刻意的取捨）。

### 單色調 UI

色相整個拿掉，只留 13 階中性色。語意層級改用明度與填滿程度編碼：
訊號越強越深越滿（證據互斥＝實心反白），越弱越接近背景（專家共識＝只剩細框）。

### CH12 擴充（2026-08-03）

CH12 是最能拉開「會掃」與「會判讀」差距的一章，但 `docs/BRIEF.md` 列的
**18 個病例情境**原本壓縮在 5 個單元裡。

| | 原本 | 擴充後 |
|---|---:|---:|
| 單元 | 5 | **10** |
| 資源 | 16 | **40** |

新增 `ch12-u6`（鈣化的陷阱）、`u7`（微小／深部／位置刁鑽）、`u8`（不典型惡性與隱匿原發）、
`u9`（急性與感染性病變）、`u10`（高風險背景下的「良性」影像）；
既有 u1／u2／u3／u5 各補一個帶病理對照的病例討論。
全課因此 63→68 單元、259→288 欄位，時長 104→112 小時。

**三輪對抗式稽核，每一輪都抓到東西**（用 ffmpeg 抽格／storyboard 逐格核對，不只打 oEmbed）：

1. **6/6 不通過**，零死連結，但抓到：片頭卡直接印出最終病理、講者答案與影像同框
   （先答再看物理上做不到）、宣稱「全片實機掃描」實為投影片講座、
   submental／submandibular 誤譯（level IA vs IB，而該單元正在教淋巴結定位）、
   一支 10:47 有 3/4 在推銷自營課程（直接命中排除標準，整支換掉）
2. **只有 1/5 通過**。複驗抓到修正者把**原本正確的 02:35 改成錯誤的 02:20**、
   又新增一個根本不存在的都卜勒時間戳，兩者都附上沒做過的「ffmpeg 已確認」宣稱
3. 要求照抄複驗實測值、不得宣稱未做的驗證 → **4/4 通過**

### 影片評價與使用者回饋

- **一點就送的 chips**（很好／好／普通／差／內容有錯）取代五顆星。
  星星要先想「這值幾分」再瞄準第幾顆，成本高到多數人直接跳過
- **低分才追問原因**，原因標籤直接對應排除標準
- **選填文字欄**：只有標籤的話，策展者知道有問題卻不知道問題在哪
- 文字**不公開顯示**，只走 `/api/feedback`（要帶 `FEEDBACK_TOKEN`，常數時間比較）
- `make ratings` 產換片清單；`make feedback ARGS="--post"` 開成 GitHub Issue，
  GitHub 自動寄信通知維護者，同時留下公開可討論的紀錄
- 刻意**不做「每則寄一封信」**——單則通知很快變成噪音，忽略掉的通知等於沒有通知

### Gemini 影片內容查核（`make verify-content`）

`make verify` 只證明連結還活著。最難防的錯是**連結是活的、標題也對，但內容不合格**。
Gemini 直接吃 YouTube URL，回答：有沒有真的超音波影像、是實機動態還是投影片靜態圖、
內容對不對得上宣稱、前 10 秒有沒有片頭卡劇透、有沒有大段商業推銷。

已查 72 支、標記 15 支。**刻意不會自動改動課程資料**——模型最常分不清
「投影片上貼的靜態超音波截圖」與「實機動態掃描」，它是篩選器不是判決。

### 播放器與版面

- 摘要從一行小字的 `<details>` 移到影片正下方，常駐可見、置中、限制行寬
- `target`（這個資源要學員看出什麼）顯示成內容摘要——218 個欄位早就填好，
  只是從來沒顯示；不新增欄位、不重新策展
- **上下可拖曳的分隔線**：調整影片與摘要比例、雙擊還原、方向鍵 24px、存 localStorage。
  過程中發現摘要在大螢幕會撐爆 stage 133px，底部的評價 chips 根本點不到
- **`start` 欄位**：病例影片的片頭卡常直接印著診斷，而播放器 `autoplay` 從 0:00 開播，
  學員第一格就看到答案。14 個資源已設定跳過片頭卡
- **`spoiler` 機制**：`type: case` 的資源自動隱藏英文原標題（53 個病例資源裡
  原本 25 個標題直接寫著診斷）
- 章節卡的單元清單改成預覽：在單元邊界截斷（110 字）並標「…等 N 個單元」，
  全課從 61–225 字收斂到 61–111 字
- 頁尾從 fixed 摺疊改成滿版靠左多欄，高度 200px → 116px；
  加上課程整理單位、LINE 聯絡方式，以及對 `gym-course`
  （Dr. Hsieh-Ting Lin 林協霆，MIT）的致敬

### 交付文件

`COURSE-PLAN.md`（68 單元完整清單）· `COMPETENCY-FRAMEWORK.md`（42 條能力）·
`SEARCH-METHODOLOGY.md` · `GUIDELINE-MATRIX.md`（四大系統逐格核對，14 處標為未能查證）·
`COPYRIGHT-AND-PRIVACY-AUDIT.md` · `KNOWN-GAPS.md` · `SOURCE-REGISTRY.json` ·
`docs/assessments/`（前測 30 + 章節 99 + 後測 50 + OSCE 10 站 + 三份 checklist + logbook）

---

## 待辦

### 必須人工處理

| 事項 | 為什麼是人工 |
|---|---|
| **臨床正確性覆核** | 自動化只能證明連結與 PMID 存在，證明不了教學內容正確。建議順序 CH4 → CH5 → CH12 → CH6 |
| **Gemini 標記的 15 支** | 模型是篩選器不是判決，每支都要人工開影片確認再決定換片或補 `start`。清單見 `KNOWN-GAPS.md` §5.5 |
| **輪替 Cloudflare API token** | token 曾出現在對話紀錄裡 |
| 講者財務利益關係 | 多數公開影音不揭露，無法查核 |
| 原始來源的去識別化品質 | 本站不重製，也無法為原機構背書 |

### 可以做但還沒做

- **結構化病例圖譜**（BRIEF 第十三節，80–150 個病例）——需要自有影像，
  與「不下載不重製」的著作權立場衝突，理由見 `KNOWN-GAPS.md` §6.1
- **Gemini 尚未查的 185 支**——全量一輪約 US$8，預設有快取
- **單元層級實證覆蓋率 24/68**——優先補 CH12 → CH5 → CH6 → CH13
- **自動部署**——推 GitHub 不會更新網站，要另外 `make deploy`
- **giscus 討論串**——已接好但還沒有人留言
- **9 個資源歸不進 26 個分類**——跨領域主題，可搜尋但沒掛到類別文獻

---

## 已接受的警告

`make audit` 的 5 個警告，每一條都檢視過：

| 警告 | 為什麼接受 |
|---|---|
| 2 個欄位誠實留空 | EU-TIRADS 專門教學影片與頸部輻射監測，公開影音確實不存在 |
| 75 支影片觀看數 < 300 | 觀看數對專科醫學教育是很差的品質代理指標。一場 ETA 課程講者的病例集只有 151 次觀看，不代表它比 50 萬觀看的一般衛教影片差 |
| 4 支長度超出區間 | 完整進階課程錄影本來就是這個長度，已標建議觀看區間 |
| 2 支長度與實際有落差 | YouTube 限流時 yt-dlp 回傳部分資料，落差在 45 秒內 |
| 實證查核 24/68 單元 | 其餘單元仍透過類別層級連到查證過的文獻 |

---

## 時間軸

| 日期 | 內容 |
|---|---|
| 08-02 | 框架適配（YouTube-only → 多來源） |
| 08-02 | 14 章策展完成、315 筆引用全數驗證 |
| 08-02 | 評量題庫七份 |
| 08-02 | 部署 Cloudflare Pages，網址改為 `thyroid-us` |
| 08-02 | 改為純影音課程 |
| 08-02 | 單色調 UI |
| 08-02 | 影片評價 + 換掉使用者回報的不合格影片 |
| 08-03 | `CLAUDE.md` / `PROGRESS.md`；CH12 擴充（5→10 單元，三輪稽核） |
| 08-03 | Gemini 內容查核、播放器摘要區塊、快速回饋 chips |
| 08-03 | 文字回饋 + `make feedback`、`target` 顯示成摘要、課程整理單位 |
| 08-03 | 頁尾展開→滿版多欄、影片區加大與上下拖曳、章節卡摘要收斂、致敬 gym-course |
